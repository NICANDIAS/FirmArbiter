# candidate.conf — Your pipeline
# Update name and id when you finalise your pipeline's name

name = Your Pipeline
id = your_pipeline
version_hint = local
success_signals = boot_success, service_reachable, emulation complete
supported_architectures = arm, mips, mipsel, x86, mips64
requires_sudo = false
